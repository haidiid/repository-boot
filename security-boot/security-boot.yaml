server:
  port: 8901

management:
  endpoints:
    web:
      exposure:
        include: '*'
    enabled-by-default: true

logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.thoughtmechanix: DEBUG

---
security-boot:
  jwt:
    secret: w1nd4nurm4l4